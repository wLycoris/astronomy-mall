<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.astronomy.mall.module.user.mapper.UserMapper">

    <!-- 基础字段 -->
    <sql id="BaseColumns">
        id, username, password, nickname, email, phone, avatar,
        interest_tags, observation_level, city, province, longitude, latitude,
        role, status, deleted, create_time, update_time, last_login_time
    </sql>

    <!-- 自定义SQL示例(如果需要) -->

</mapper>